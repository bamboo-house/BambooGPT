import { TopBody } from './TopBody';
import { TopHeader } from './TopHeader';

export const Top = () => {
  return (
    <div className="top-main">
      <TopHeader />

      <TopBody />
    </div>
  );
};
